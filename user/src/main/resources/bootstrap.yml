server:
  port: 8082  #端口
  tomcat:
    uri-encoding: UTF-8   #服务编码
spring:
  profiles:
    active: my
  application:
    name: user-service
  cloud:
    nacos:
      config:
        file-extension: yaml
        shared-configs: # 共享配置
          - data-id: shared-spring.yaml # 共享spring配置
            refresh: false
          - data-id: shared-redis.yaml # 共享redis配置
            refresh: false
          - data-id: shared-mybatis.yaml # 共享mybatis配置
            refresh: false
          - data-id: shared-logs.yaml # 共享日志配置
            refresh: false
          - data-id: shared-feign.yaml # 共享feign配置
            refresh: false
          - dataId: shared-mq.yaml # 共享mq配置
        timeout: 10000
        config-retry-time: 3000
xy:
  swagger:
    enable: true
    enableResponseWrap: true
    package-path: com.aynu.user.controller
    title: 校园物品共享平台 - 用户中心接口文档
    description: 该服务用于用户管理、用户信息管理
    contact-name: 安阳师范学院
    version: v1.0
  auth:
    resource:
      enable: true
      excludeLoginPaths:
        - /users/register
        - /users/detail/{isStaff}
        - /users/list
        - /users/{id}/type
        - /users/ids
  jdbc:
    database: xy_user
